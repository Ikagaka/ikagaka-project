{
  "name": "ikagaka-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "node prepare.js",
    "bootstrap": "npm run installAll && npm run prepareAll",
    "installAll": "lerna bootstrap --ignore-scripts && node bootstrapFix.js",
    "prepareAll": "lerna run prepublish && lerna run prepare",
    "depver": "node dependencyVersions.js",
    "multi-depver": "node dependencyVersions.js --ignore-single",
    "gen-sln": "node genSln.js"
  },
  "author": "narazaka",
  "license": "MIT",
  "devDependencies": {
    "fso": "^2.5.15",
    "lerna": "lerna/lerna#master",
    "lnk": "^1.1.0",
    "uuid": "^3.2.1"
  }
}
