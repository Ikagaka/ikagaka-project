{
  "name": "ikagaka-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "node prepare.js",
    "bootstrap": "lerna bootstrap --ignore-scripts && node bootstrapFix.js && lerna run prepublish && lerna run prepare"
  },
  "author": "narazaka",
  "license": "MIT",
  "devDependencies": {
    "fso": "^2.5.15",
    "lerna": "lerna/lerna#master",
    "lnk": "^1.1.0"
  }
}
